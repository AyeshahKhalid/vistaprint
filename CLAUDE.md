# PRINTGRAPHI (Vistaprint clone) — Project Notes

This is a Vistaprint-style e-commerce site (Angular frontend + NestJS backend) with a clear split of source-of-truth:

- **Functionality, UX flow, navigation, cart/checkout behavior, product configurator behavior**: follow the real vistaprint.com site as the blueprint.
- **Visual theme, design, content, copy, images**: follow the Figma file instead, NOT vistaprint.com's current look. Figma file: https://www.figma.com/design/VS9tmoBlIrg612RHABLAkt/Untitled (file key `VS9tmoBlIrg612RHABLAkt`).

**Why:** Client confirmed the Figma design should replace Vistaprint's visual design/content entirely, while the site should still function/flow like vistaprint.com since Figma has no interactive flow documentation (no prototype links, no annotations) — just static page mockups per category.

**Fidelity level**: "Exact Figma design" means close visual match — real content, images, colors, and layout structure from Figma, NOT pixel-exact spacing/sizing. Flow/behavior (navigation, mega-menu interaction, routing between pages) should mirror vistaprint.com's actual UX, not Figma's static mockups.

**Brand name in design: "PRINTGRAPHI"** (confirmed from homepage text content — header nav, footer copyright, ambassador spotlight copy, etc.). Occasional leftover "VistaPrint" text in the design is a placeholder inconsistency — PRINTGRAPHI is correct everywhere.

## Figma file structure

- Page 1 canvas: business card sub pages (38 page variants), Postcards & Print Advertising, main sections (homepage), Custom Signs/Banners/Posters, Labels & Stickers, Packaging, Clothing
- 🧩 Components canvas: **UNRELATED leftover content** from a different template ("Moxie Glow Collective" salon/spa brand) — do NOT use these components. The real header/footer/nav used throughout every PRINTGRAPHI page are nested directly inside each page frame (`top-utility-bar`, `main-header`, `category-navigation-bar`, `promo-announcement-bar`, `footer`).
- Promotional Products canvas: ~50 product pages
- Invitations & Stationery canvas: ~40 product pages
- Wedding canvas: ~40 product pages

Each category follows the same IA pattern: Landing Page → Sub-category Product Page → Material/Style Variant Pages → Shape/Specialty Variant Pages → Service Pages (next-day, reorder, sample kit).

## Build status

**All 11 top-level category pages complete**: Home (`/`), Business Cards (`/business-cards`), Postcards & Print Advertising (`/postcards-print-advertising`), Signs Banners & Posters (`/signs-banners-posters`), Labels & Stickers (`/labels-stickers`), Clothing & Bags (`/clothing-bags`), Promotional Products (`/promotional-products`), Packaging (`/packaging`), Invitations/Gifts/Stationery (`/invitations-gifts-stationery`), Wedding (`/wedding`), Design Services (`/design-services`). All verified with clean `ng build` and live dev-server 200s + spot-checked images.

**Business Cards sub-page variants (product detail pages)**: Figma's "business card sub pages" frame (node `275:4`, under Page 1) has 38 children — 1 duplicate of the category page already built, and 36 real PDPs for material/shape/use variants (matte, glossy, uncoated, premium, natural-textured, soft-touch, cotton, linen, kraft, rounded-corner, square, leaf, oval, circle, painted-edge, ultra-thick, plastic, foil-accent, raised-foil, embossed-gloss, premium-plus, qr-code, magnetic, loyalty, appointment, stickers, next-day, 2-day, holders, packaging-insert, free-sample-kit, reorder, standard, deluxe, fast-delivery).

**All 35 Business Cards variants and all 47 Postcards & Print Advertising variants are live (2026-09-18)** via the catalogue architecture below. Matte keeps its hand-written Figma-verified `matte.data.ts`; every other variant is generated from a `VariantSpec` (copy and prices are placeholder — written by Claude, not from Figma — and all variants share the category's images until per-variant assets exist). The remaining 9 categories (~368 nav links) still 404 and follow the same recipe.

Each PDP follows an identical structural pattern (confirmed from Matte, node `55:4`): breadcrumb → hero (image gallery + purchase configurator: badges, title, star rating, price, delivery estimates, shape/corner dropdowns, quantity tiers, CTA buttons, design-service upsell) → product tabs (Samples/Specs & Templates/Product Options) → popular templates carousel → feature highlights → image+text description → "personal touch" thickness/finish options → "start designing" image+text → "Better by Design" sustainability blurb → related products → frequently bought together → customer reviews (rating breakdown bars + review cards) → design-services banner → FAQ.

**Build approach (catalogue-driven, mirrors how vistaprint.com serves one template per product)**: ONE shared `ProductDetail` component (`frontend/src/app/shared/product-detail/`) renders every variant from `ProductDetailData`. Per category you add exactly three things: (1) a `<category>.base.ts` (structural base page — Matte's data serves as Business Cards' base), (2) a `<category>.catalog.ts` listing one `VariantSpec` per slug and calling `buildCatalog()` from `shared/product-detail/variant-catalog.ts`, (3) one entry in `shared/product-detail/catalog-registry.ts` (dynamic `import()` so each catalogue is its own lazy chunk) plus a `...category('<path>', '<title>', loader, true)` line in `app.routes.ts`. There are NO per-variant wrapper components: the route `<category>/:slug` uses the shared `ProductVariantPageComponent` with `productVariantResolver`, which loads the catalogue on demand, binds the product via `withComponentInputBinding()`, and returns a `RedirectCommand` to the category page for unknown slugs. Check a new catalogue with a Node script that reads nav slugs from `nav-data.ts` and calls `loadProduct()` for each (every nav slug must resolve; no extras).

**Routing/perf (2026-09-18)**: every route is `loadComponent` (initial bundle 743 kB → 299 kB), `withPreloading(PreloadAllModules)` fills the rest in the background, `withInMemoryScrolling` restores scroll and handles `fragment` anchors, a custom `PageTitleStrategy` sets document titles (resolved product title on variant/studio pages), and `**` routes to `NotFoundComponent`. Budget is tightened to 400 kB warn / 800 kB error — keep it there.

**Purchase flow (client-side until the backend exists)**: PDP configurator is live (quantity tier → price, dropdowns, zip edit, show-more tiers). **"Upload your design" does NOT navigate directly** — per vistaprint.com it opens a right-side options drawer on the PDP (shape/corner option groups pre-filled, price summary, **Next**), and only Next hands over to `/studio/:category/:slug?mode=upload&qty=…&<Option>=…`. Category pages deep-link into that drawer with `?design=upload` on the PDP. "Browse our templates" goes straight to the studio in templates mode, as vistaprint's does. `DesignStudioComponent` is a **real canvas editor (stage 1, built 2026-09-18) on Fabric.js v6** — the only runtime dependency added, lazy-loaded with the studio chunk only (initial bundle unaffected). It has its own chrome (no site shell), modelled on vistaprint.com/studio: top bar (product, Save, Undo/Redo, Preview, price, Next) · tool rail (Product options, Text, Uploads, Graphics, Background, Template; Pattern is a disabled placeholder) · canvas at real size (250 px/in at 100%, trim + 0.0625" bleed, 0.125" safety; dims from the Size option or 3.5×2 / 6×4 by category) with toggleable Bleed/Safety guides and rulers · zoom bar. Undo/redo is JSON snapshots (`canvas.toJSON`) with guides `excludeFromExport` and re-added after every `loadFromJSON`. Save → localStorage `printgraphi.studio.<category>/<slug>` and is restored on reopen ("Edit design" from the cart). Next renders a PNG preview (`toDataURL`, guides hidden) into the cart item (`preview`, `design: { type: 'studio' }`). Later stages: Pattern, graphics/asset library, front/back sides, PDF/AI upload, editable layered templates. → `CartService` (signals + localStorage, `printgraphi.cart`) → `/cart` (header cart icon shows a count badge). Checkout is intentionally disabled pending sign-in/payment scope. These two pages have no Figma reference; they follow the existing token/`.btn` language and are the only new visual surfaces — flag to the client.

**Figma access status (2026-09-18)**: the claude.ai Figma MCP connector returned "no edit access" for the main file, then disconnected entirely when the user's account switched mid-session — it must be reconnected from claude.ai → Connectors → Figma before any design data can be pulled. No cached Figma JSON survives on disk (per-session scratchpads are gone). The REST node-data endpoints were also rate-limited earlier (file-level, ~3.5 day cooldown). **A second Figma file for the ADMIN flow (admin login + admin screens) was shared: `hPioZIeyB5A7mfbnBPD8Kr` ("Printgraphi", node `0-1`) — unread so far; assess it as soon as the connector is back.** Client-facing scope questions live in `SCOPE-GAPS.txt` at the repo root (design studio, auth, cart/checkout, Deals listing, admin, search).

## Figma API gotchas (important — avoid re-discovering these)

- **Image export rate limit**: the standard `/v1/images/:file_key?ids=...` render/export endpoint has a FILE-LEVEL rate limit (not per-token). Once exhausted it stays exhausted for days regardless of token. **Fix**: use `/v1/files/:file_key/images` instead — returns a `meta.images` map of every raster image fill, keyed by `imageRef` hash, as direct S3-signed URLs, NOT subject to this limit. Workflow: fetch this map once, capture each image node's `imageRef` while walking the tree, look up `imageMap[imageRef]` directly.
- **Node-data rate limit**: separately, `/v1/files/:key/nodes` and `/v1/files/:key?ids=...` (structure/text fetches) can ALSO become rate-limited on a per-file basis. If this happens, no new page content can be fetched until it resets — fall back to polishing/fixing already-built pages using cached data instead of guessing.
- The "🧩 Components" canvas nodes are a trap — they belong to a different, unrelated design (salon/spa "Moxie"). Always use the header/footer/nav nested in each actual page frame.
- Breadcrumb depth and header pattern **varies per category page** — some have no breadcrumb at all (Signs Banners & Posters uses a page-title bar + in-page nav grid instead). Never assume the previous page's pattern applies; check each page's own top-of-page frames.
- **FAQ pattern**: Figma consistently writes a real answer for only the FIRST FAQ question (sometimes with a structured table, e.g. Business Cards' sizes table); all subsequent questions are collapsed placeholders with no answer text. The `FaqAccordion` component's `answer` field is optional for this reason — leave undefined rather than fabricating content.

## Styling / color system

`frontend/src/styles/_variables.scss` holds all design tokens. Most pages share a base navy/blue palette (`$color-primary` #167ef5, `$color-text-body` #374151, etc.), but **some pages have genuinely distinct palettes per Figma** — always extract a page's own colors from its Figma data before styling, never assume the base palette applies:

- **"Modern blue" theme** (`$color-modern-text` #1a1a2e / `$color-modern-accent` #2563eb): shared across Packaging, Invitations, and Design Services.
- **Promotional Products**: distinct warm/stone neutrals (`#57534e`, `#78716c`, `#f5f5f4`, `#fafaf9`), orange accent `#ea580c`, dark near-black tones `#0f172a`/`#1a1a2e`. Prefixed `$color-promo-*`.
- **Wedding**: warm-stone palette with pink/green accents (`$color-wedding-*`), including a separate `$color-wedding-text-body` (#57534e) distinct from `$color-wedding-text-secondary` (#44403c) — easy to conflate, verified distinct in Figma.

**Before writing any new page's SCSS**: run a color-extraction pass against that page's own Figma data first (walk the tree, convert fills to hex, print alongside node path) and map to the closest existing token — add a new one only if a genuinely distinct color exists.

## Known bug classes to watch for

1. **"Component" text-leak bug**: an earlier bulk rename (see "File naming convention" below) accidentally corrupted visible page COPY, not just code — e.g. "PackagingComponent", "WeddingComponent" appearing as literal text in breadcrumbs/labels/nav. Run this sweep after any future bulk rename/replace: `grep -rn "'[A-Za-z ]*Component'" --include="*.ts" frontend/src/app` and check the shared `nav-data.ts` too (it's global, affects every page).
2. **Missing icons**: "why us"/benefit-icon sections sometimes have an icon-circle div with no SVG child — always check Figma's icon node name (star/users/truck/box/palette/etc.) and add a matching inline SVG.
3. **Bare-number ratings**: some pages had numeric ratings with no visual star icons — Figma always shows actual star vector icons (filled/empty per rating value).
4. **Generic ProductCard misuse**: the shared `ProductCard` component matches a specific spec (white bg + border + radius, 5-icon SVG star row, divider before pricing, bordered CTA button) — don't approximate this with plain text.

## File naming convention

Every Angular component uses the classic `.component` suffix: `name.component.ts/.html/.scss`, exported class `NameComponent` (e.g. `business-cards.component.ts` exports `BusinessCardsComponent`). Applies to the root `app.component.ts` (`AppComponent`) too. Selectors (`app-business-cards`, etc.) are unaffected. Use this suffix for every new component.

## Nav mega-menu

Data-driven via `frontend/src/app/shared/nav-data.ts` (`NAV_CATEGORIES`). Real mega-menu column data exists for Business Cards, Postcards & Print Advertising, and Signs Banners & Posters. Remaining categories (Labels & Stickers, Clothing & Bags, Promotional Products, Packaging, Invitation, Wedding, Design Services) have no mega-menu data yet.

Behavior: desktop opens on pure CSS `:hover`/`:focus-within` (zero JS cost), click always navigates to the category's own page (matches vistaprint.com), touch devices get a signal-based toggle fallback via `matchMedia('(hover: hover)')`.

## Reusable components

Shared UI pieces live in `frontend/src/app/shared/`: `SiteShell`, `MainHeader`, `TopUtilityBar`, `CategoryNavBar`, `PromoAnnouncementBar`, `SiteFooter`, `ChatButton`, `MegaMenu`, `SectionHeader`, `ProductCard`, `Breadcrumbs`, `FaqAccordion`, `ProductDetail`. All use `ChangeDetectionStrategy.OnPush` and Angular signals (not manual subscriptions). Reuse these when building further pages instead of duplicating markup.

## Open items / not yet done

- ~~35 of 36 Business Cards PDP variants still need building.~~ **Done 2026-09-18** (plus all 47 Postcards variants). Still to catalogue: Signs/Banners/Posters (55 nav slugs), Labels & Stickers, Packaging, Clothing & Bags, Promotional Products, Invitations/Gifts/Stationery, Wedding, Logo/Websites/Social, Design Services — ~368 nav links 404 until then. Each category page's own `href="#"` buttons should be wired to its variant routes at the same time (Business Cards and Postcards pages are fully wired; other pages still have 1–9 dead links each).
- **Nav rebuilt from the client sitemap 2026-09-18** (`nav-data.ts`): 13 top-level categories in vistaprint.com's order, correct labels ("Invitations, Gifts & Stationery", "Retail, Food & Shipping Packaging", "Signs, Banners & Posters"), new "Logo, Websites & Social" category, Deals mega-menu, and sitemap-accurate columns (Business Cards is 8 columns, not 4). 462 distinct nav paths total.
- Account layer (Sign In, My Account, My Projects, Favorites, Help Center, footer/utility links, Postcards "Let Us Help" row) and site search are intentionally unwired — they need the auth/backend decision in `SCOPE-GAPS.txt`.
- ~~Mega-menu data missing for 7 of 10 nav categories.~~ **Fixed 2026-09-17**: all 10 categories now have real mega-menu data in `nav-data.ts`. The 3 built from Figma (Business Cards, Postcards, Signs/Banners/Posters) were kept as-is; the other 7 (Labels & Stickers, Clothing & Bags, Promotional Products, Packaging, Invitation, Wedding, Design Services) were sourced from vistaprint.com's actual mega-menu structure (Figma has no mega-nav frame for these).
- ~~Home's "explore all categories" is a static grid but Figma shows scroll chevrons~~ **Fixed and dimension-verified 2026-09-17**: rebuilt as a 150x189 bordered white card (12px radius, `#d9e0e8` border, 150x140 image + 150x49 title container) inside a horizontally-scrollable track with working 36x36 circular prev/next arrow buttons (`scrollCategories()` in `home.component.ts`, `.categories-track`/`.categories-arrow`/`.category-tile` in `.scss`). First "fix" pass only addressed scroll behavior, not the card's actual shape/border/container — user caught this ("this section design is not matching with figma. how you can say that design is same"), which prompted a full re-verification method (see below).
- **All 11 pages (Home + 10 category pages) dimension-level Figma verification completed 2026-09-17.** Method: extract exact `absoluteBoundingBox`/`fills`/`cornerRadius`/`strokes` from cached Figma JSON per section (throwaway Node scripts reading the scratchpad JSON), diff directly against SCSS/HTML — not screenshot comparison. Prompted by direct user pushback that earlier "matches the design" claims were not actually verified ("can you check by yourself on design by checking code"). Full `ng build` confirmed clean after all fixes landed.
  - **Systemic bug found on nearly every page**: card-style repeating sections (collections, ideas, ambassador/testimonial cards, service cards, category tiles, bestseller cards, etc.) were built as loose image+text stacks missing Figma's actual white-bg + bordered (`#e5e7eb`/`#e7e5e4`/`#d9e0e8` depending on page) + border-radius container. Fixed across Home, Business Cards, Postcards, Signs/Banners/Posters, Labels & Stickers, Clothing & Bags, Promotional Products, Packaging, Wedding, Design Services, and Invitations.
  - ~~Postcards/Signs bestseller carousels are missing prev/next arrow controls shown in Figma.~~ **Fixed**: added real 36-40px circular prev/next arrow buttons to Postcards, Signs/Banners/Posters, and Invitations bestseller/category carousels.
  - ~~Wedding's design-help section layout wasn't fully verifiable from cached data depth~~ **Now verified**: confirmed a two-column image/dark-navy-panel split with a white pill CTA — matches what was already built structurally; only minor height tweaks needed.
  - Other bug classes found and fixed during this pass: unicode star/checkmark glyphs used instead of real SVG icons (Business Cards, Clothing & Bags — known bug class #3); a "Component" text-leak recurrence on Invitations ("HomeComponent and Gifts", "WeddingComponent Invites") — same bug class as #1, re-swept and fixed; wrong page-specific accent colors (Design Services was using the shared `$color-modern-accent` instead of its own `#064fe0`, now `$color-design-services-accent`); wrong image aspect ratios/grid column counts on several pages (Wedding's fresh-arrivals was a 6-column grid instead of 3; various card images were square when Figma specifies non-square ratios); missing page-title-bar dividers (Postcards); missing mega-nav "Shop all X" CTA links (Signs/Banners/Posters, simplified to one per column — Figma's nested multi-CTA-per-column pattern wasn't modeled, would need a data-model change to fully replicate).
  - Work was parallelized across background agents (one per page) using cached Figma JSON already in the scratchpad — no fresh Figma API calls were needed, so the node-data rate limit was not a blocker for this pass.
- No formal flow/UX doc from the client yet — flow assumptions are based on vistaprint.com's actual site behavior.

## Deals page

**Figma confirmed to have NO dedicated Deals page** — the "Deals" nav item's Figma frame is content-identical to the Homepage (same hero/sections), just with "Deals" shown as the active nav item (colored `$color-primary` #167ef5, NOT red — this was a real bug: the nav's `highlight` treatment was originally coded as red `#d0021b`, which was wrong; fixed to blue to match Figma's actual color).

Implementation: `/deals` route reuses `HomeComponent` directly (`{ path: 'deals', component: HomeComponent }` in `app.routes.ts`), since the content is identical. Proper active-nav-link highlighting was added using Angular's `RouterLinkActive` (`category-nav-bar.component.html`/`.ts`/`.scss`) instead of the old static `highlight` flag hack — this means every nav item now correctly shows as active on its own page, not just Deals.

**Note**: vistaprint.com's real `/deals` is a different, more complex pattern (category carousel + sidebar filters + paginated product grid of dozens of items) — if the client wants that fuller filterable-listing experience instead of the Figma-matching homepage-reuse version, that's a larger feature (filtering logic, pagination) to scope separately.
